
package states;

import Util.Token;
import Util.Type;

public class state2
    {
        public Token takeAChar(char c)
        {
            if (c == '=')
            {
               // return new Token(Type.OP_EQ, "==", row, column); 
            }
            
            else
            {
         //    return new Token(Type.OP_ASS, "=", row, column);  
                   
            }
            return null;
       
        }
    }
